package com.loparok.repository;

import com.loparok.model.Customer_have_discount;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Customer_have_discountRepository  extends JpaRepository<Customer_have_discount, Long> {
}
