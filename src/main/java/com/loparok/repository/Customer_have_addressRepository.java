package com.loparok.repository;

import com.loparok.model.Customer_have_address;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Customer_have_addressRepository  extends JpaRepository<Customer_have_address, Long> {
}
